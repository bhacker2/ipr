<%= form_for @business_area, :html => { :class => "form-horizontal business_area" } do |f| %>

    <% if @business_area.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@business_area.errors.count, "error") %> prohibited this business_area from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @business_area.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@business_area[:name]) %>
  </div>
  <div class="control-group">
    <%= f.label :director, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :director, :class => 'form-control' %>
    </div>
    <%= error_span(@business_area[:director]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            business_areas_path, :class => 'btn btn-default' %>

<% end %>
